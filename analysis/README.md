# Analysis

Analysis of saved activation files.

Note that this does not encompass training or analysis of the completed models themselves. For this, please see the `scripts/` directory with a particular focus on the `evaluate_probe_roc.py` script.

This assumes that activations are stored as `.npz` files in some common directory. See the below standards doc to see how this data is expected to be stored.
